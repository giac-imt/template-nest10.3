module.exports = {
    pattern: '^(|feat|fix|hotfix|release|ops|config)/.+',
    errorMsg:
        '🤨 The branch name is incorrect, rename it with `git branch -m <old-name> <new-name>` following this pattern ^(feat|fix|hotfix|release|ops|config)/.+',
}
